<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>FDA Label Watch — recent label changes</title>
  <link rel="self" href="feed.xml"/>
  <link href="index.html"/>
  <id>urn:fda-label-watch</id>
  <updated>{{ generated_at.strftime('%Y-%m-%dT%H:%M:%SZ') }}</updated>
  <generator>fda-label-watch</generator>
  {% for c in changes %}
  <entry>
    <title>{{ c.drug_name }} — {{ c.section_label }} (v{{ c.from_version }} → v{{ c.to_version }})</title>
    <id>urn:fda-label-watch:{{ c.drug_slug }}:{{ c.section_slug }}:{{ c.to_version }}</id>
    <updated>{{ c.when.strftime('%Y-%m-%dT%H:%M:%SZ') }}</updated>
    <link href="drugs/{{ c.drug_slug }}/index.html#change-{{ c.section_slug }}-{{ c.to_version }}"/>
    <summary type="html">{{ c.diff_html }}</summary>
  </entry>
  {% endfor %}
</feed>
